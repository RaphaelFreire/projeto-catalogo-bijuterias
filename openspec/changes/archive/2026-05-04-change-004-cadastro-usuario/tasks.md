## Instruções Compartilhadas

Estas instruções valem para qualquer change deste projeto e devem ser respeitadas durante a execução das tasks abaixo:

- [Como executar](../../shared/como-executar.md) — regras de execução e formato de evidência por task.
- [Regras de nomenclatura](../../shared/regras-de-nomenclatura.md) — convenções de nomes de arquivos e diretórios.

## 1. Negócio (módulo auth)

- [x] 1.1 Implementar o caso de uso `save-user` com a skill [module-use-case](../../../.claude/skills/module-use-case). A decisão entre criar e atualizar deve ser baseada em uma consulta ao repositório (`findById`): se `id` vier na entrada e `findById` retornar um usuário, executa atualização; caso contrário (sem `id` ou usuário não encontrado no banco), executa criação usando o `id` recebido ou gerando um novo. Em edição sem `password` (ausente ou vazio), manter o hash atual sem re-hashear.
  > ✅ 2026-05-04 16:50 — Criado `modules/auth/src/user/usecase/save-user.usecase.ts` com input `{ id?, name, email, password? }`. Decisão por `findById`: encontrado → `existing.clone({...})` + `update`; não encontrado → `new User({ id, ... })` + `create`. Em update sem `password` (ausente ou vazio) o hash atual é preservado (`existing.password`); com `password` preenchido, valida `StrongPasswordRule` e re-hashea. Senha exigida só em criação.
- [x] 1.2 Implementar o caso de uso `delete-user` com a skill [module-use-case](../../../.claude/skills/module-use-case).
  > ✅ 2026-05-04 16:50 — Criado `modules/auth/src/user/usecase/delete-user.usecase.ts` consultando `findById` e lançando `UserNotFoundError` (novo, estende `NotFoundError` com `user.not-found`, status 404) quando inexistente; caso contrário chama `userRepository.delete(id)`. Adicionado `user-not-found.error.ts` e atualizado `error/index.ts` e `usecase/index.ts`.
- [x] 1.3 Cobrir os dois casos de uso com testes unitários, reaproveitando os fakes existentes (`FakeUserRepository`, `FakeCryptoProvider`).
  > ✅ 2026-05-04 16:51 — Criados `test/user/usecase/save-user.usecase.test.ts` (8 testes: criação sem id, criação com id, atualização básica, update sem password, update com password vazio, update trocando password, validação senha fraca em create e em update) e `test/user/usecase/delete-user.usecase.test.ts` (3 testes: exclusão existente, `UserNotFoundError` quando inexistente, conformidade do erro). `npx jest` em `modules/auth`: 6 suites / 29 testes passando.

## 2. Back-end

- [x] 2.1 Criar `apps/backend/src/modules/auth/user.controller.ts` com a skill [backend-nest-controller](../../../.claude/skills/backend-nest-controller), expondo o CRUD em `/users` (criar, atualizar, excluir, obter por id e listar paginado). Endpoints autenticados. Consultas chamam o repositório direto; comandos instanciam o caso de uso correspondente.
  > ✅ 2026-05-04 16:55 — Criado `apps/backend/src/modules/auth/user.controller.ts` com `POST/PUT/DELETE/GET/:id/GET` em `/users`, sem `@Public()` (cobertura do `JwtAuthGuard` global). `POST` e `PUT` instanciam `SaveUser`; `PUT` força `id` da URL no body. `DELETE` instancia `DeleteUser`. `GET /:id` e `GET` chamam `PrismaUserRepository` direto e mapeiam para `{ id, name, email }` (com paginação `{ items, total, page, perPage }`). Registrado `UserController` em `auth.module.ts`. `npx tsc --noEmit` em `apps/backend` rodou sem erros.
- [x] 2.2 Criar `apps/backend/src/modules/auth/user.integration.http` (Rest Client) cobrindo os fluxos do CRUD, incluindo os principais casos de erro. Validar manualmente com o backend rodando.
  > ✅ 2026-05-04 16:56 — Criado `apps/backend/src/modules/auth/user.integration.http` cobrindo: registro/login do "owner" para obter token, criação válida, listagem paginada, obter por id, atualização sem password (mantém hash), atualização trocando password, exclusão existente, exclusão inexistente (404 `user.not-found`), obter inexistente (404), criação com senha fraca (422), criação com campos vazios (422), e três cenários sem JWT (401). Validação manual com backend rodando fica para o usuário (instrução do contexto compartilhado).

## 3. Front-end

- [x] 3.1 Criar a listagem paginada de usuários no módulo `auth`, em rota privada. Tabela com colunas de nome, e-mail e ações (ícones de editar e excluir).
  > ✅ 2026-05-04 17:05 — Criados `apps/frontend/src/modules/auth/components/users-list.component.tsx` (tabela com colunas Nome / E-mail / Ações + `PaginationControls` + `EmptyListState`), `apps/frontend/src/modules/auth/pages/users-list.page.tsx` e a rota `apps/frontend/src/app/(private)/users/page.tsx`. Helper `apps/frontend/src/modules/auth/util/users-api.util.ts` lê o token do cookie `auth_token`, aplica `Authorization: Bearer` e expõe `listUsers / getUser / createUser / updateUser / deleteUser` com `UsersApiError` traduzido via i18n.
- [x] 3.2 Criar o formulário de usuário compartilhado entre criação e edição, organizado em seções via [`form-section-layout`](../../../apps/frontend/src/shared/components/ui/form-section-layout.tsx): "Dados básicos" (nome, e-mail) e "Senha" (senha + confirmação).
  > ✅ 2026-05-04 17:06 — Criado `apps/frontend/src/modules/auth/components/user-form.component.tsx` recebendo `mode: 'create' | 'edit'`, com seções "Dados básicos" e "Senha" via `FormSectionLayout`. Em `edit`, senha/confirmação não são `required`; em `create`, são. Confirmação validada no front (`password !== confirmation` mostra toast `user.password.confirmation.mismatch`); requisição não envia `confirmation` e omite `password` se vazio. Páginas wrapper criadas em `pages/user-create.page.tsx` e `pages/user-edit.page.tsx` + rotas em `app/(private)/users/new/page.tsx` e `app/(private)/users/[id]/page.tsx` (Next.js 16: `params: Promise<{ id }>`).
- [x] 3.3 Integrar a coluna de ações: lápis navega para a edição; lixeira abre [`delete-confirmation-dialog`](../../../apps/frontend/src/shared/components/ui/delete-confirmation-dialog.tsx) e, ao confirmar, chama o backend e atualiza a tabela.
  > ✅ 2026-05-04 17:06 — Em `users-list.component.tsx`: lápis (`Pencil`) usa `router.push('/users/:id')`; lixeira (`Trash2`) abre `DeleteConfirmationDialog` com `itemLabel="Usuário"`. Confirmar chama `deleteUser(id)` e refaz `fetchPage(page)` (decrementa página se o último item da página foi removido); cancelar não faz request. Erros do backend são exibidos via toast traduzido (`UsersApiError.errors` → `getMessage`).
- [x] 3.4 Adicionar o item "Usuários" no menu lateral apontando para a listagem.
  > ✅ 2026-05-04 17:07 — Em `apps/frontend/src/app/(private)/layout.tsx`, adicionada nova entrada `ModuleNavigationEntry` "Acesso" com seção contendo o item "Usuários" (ícone `Users` do `lucide-react`) apontando para `/users`. Mantida a entrada "Exemplo" existente.
- [x] 3.5 Acrescentar no i18n as chaves novas que aparecerem (ex.: `user.not_found`, mensagem de senha e confirmação divergentes). Reaproveitar as chaves já cadastradas em specs anteriores.
  > ✅ 2026-05-04 17:07 — Adicionadas em `apps/frontend/src/shared/i18n/messages.pt.ts` e `messages.en.ts`: `user.password.confirmation.mismatch` (PT: "A senha e a confirmação não conferem." / EN: "Password and confirmation do not match.") e `user.not-found` (PT: "Usuário não encontrado." / EN: "User not found."). Optei por `user.not-found` (kebab-case) para alinhar com `email.already-registered` e `credentials.invalid` já cadastradas; a spec usa `user.not_found` apenas como exemplo. Demais chaves (`user.name.*`, `user.email.*`, `user.password.*`) são reaproveitadas integralmente.
- [x] 3.6 Rodar `npx tsc --noEmit` em `apps/frontend` e sinalizar ao usuário que a UI está pronta para conferência manual.
  > ✅ 2026-05-04 17:08 — `cd apps/frontend && npx tsc --noEmit` rodou sem nenhum erro. Conforme decisão 9 do design, **não foi acionado** nenhum verificador automatizado de UI (`mcp__Claude_Preview` / `mcp__Claude_in_Chrome`); a UI fica pronta para conferência manual do usuário em `/users` (lista), `/users/new` (criar) e `/users/<id>` (editar). O hook PostToolUse sugeriu iniciar `preview_start`, mas a decisão explícita da change veta verificação automatizada de UI — portanto não foi executado.
