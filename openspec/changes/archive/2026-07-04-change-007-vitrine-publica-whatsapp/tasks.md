## Instruções Compartilhadas

Estas instruções valem para qualquer change deste projeto e devem ser respeitadas durante a execução das tasks abaixo:

- [Como executar](../../shared/como-executar.md) — regras de execução e formato de evidência por task.
- [Regras de nomenclatura](../../shared/regras-de-nomenclatura.md) — convenções de nomes de arquivos e diretórios.

## 1. Negócio — módulo novo `settings`, agregado singleton `store-settings`

- [x] 1.1 Criar o módulo `settings` com a skill [config-new-module](../../../.claude/skills/config-new-module).
  > ✅ 2026-07-04 — Executado `node .claude/skills/config-new-module/scripts/create-module.js --module settings --namespace @sdd`. **Desvio conhecido do script** (mesmo já visto em changes anteriores): `modules/settings/package.json` veio com o placeholder `__package_name__` não substituído (`@sdd/__package_name__`), corrigido manualmente para `@sdd/settings`. `npm install`, `npm run build` e `npm run test --workspace @sdd/settings` ok após a correção.
- [x] 1.2 Criar o agregado `store-settings` dentro do módulo `settings` com a skill [module-aggregate](../../../.claude/skills/module-aggregate).
  > ✅ 2026-07-04 — Executado `node .claude/skills/module-aggregate/scripts/create-aggregate.js --module settings --aggregate store-settings --mode crud`.
- [x] 1.3 Implementar a entidade `StoreSettings` com a skill [module-entity](../../../.claude/skills/module-entity), com o campo `whatsappNumber` (string, required, validado com `PhoneRule` — formato E.164).
  > ✅ 2026-07-04 — `store-settings.entity.ts` com `validate()` usando `RequiredRule` + `PhoneRule` (`@sdd/shared`).
- [x] 1.4 Definir o contrato do repositório de `store-settings` com a skill [module-repository](../../../.claude/skills/module-repository), no formato `CrudRepository` padrão (mesmo que só parte dos métodos seja exposta via HTTP).
  > ✅ 2026-07-04 — `StoreSettingsRepository extends CrudRepository<StoreSettings, StoreSettings, StoreSettings, StoreSettingsPageParams>` (gerado pelo scaffold, já no formato correto, sem alteração necessária).
- [x] 1.5 Definir a constante `STORE_SETTINGS_ID` (id fixo do registro único) exportada pelo agregado, e implementar o caso de uso `save-store-settings` com a skill [module-use-case](../../../.claude/skills/module-use-case): decide criar/atualizar via `findById(STORE_SETTINGS_ID)`, sempre usando esse id fixo (nunca vindo do chamador).
  > ✅ 2026-07-04 — `STORE_SETTINGS_ID = "00000000-0000-4000-8000-000000000000"` (formato uuid v4 válido, sentinela fixa) exportada de `store-settings.entity.ts`. `save-store-settings.usecase.ts` consulta `findById(STORE_SETTINGS_ID)` e cria/atualiza sempre com esse id. Removidos os usecases CRUD genéricos gerados pelo scaffold (`create/update/delete/find-by-id/find-page`) — nenhum deles serve ao padrão singleton; nem `delete-store-settings` foi mantido, já que não existe cenário de excluir o único registro (decisão registrada no design.md).
- [x] 1.6 Cobrir o caso de uso com testes unitários, usando `FakeStoreSettingsRepository`. Cenários mínimos: criação do registro quando não existe, atualização quando já existe, validação de `whatsappNumber` inválido.
  > ✅ 2026-07-04 — `FakeStoreSettingsRepository` em `modules/settings/test/mock`; suíte cobre criação, atualização, validação de `whatsappNumber` ausente e fora do formato E.164. `npm run test --workspace @sdd/settings` → 5 testes passando. `npm run build --workspace @sdd/settings` ok.

## 2. Back-end — `settings`

- [x] 2.1 Sincronizar o módulo `settings` com o Prisma criando o model `StoreSettings` com a skill [backend-prisma-sync-module](../../../.claude/skills/backend-prisma-sync-module).
  > ✅ 2026-07-04 — Model `StoreSettings` (`@@map("store_settings")`) em `apps/backend/prisma/models/settings.model.prisma`. Migration `20260704183741_settings_store_settings` gerada via `npx prisma migrate dev --name settings-store-settings` e aplicada.
- [x] 2.2 Implementar o repositório Prisma de `store-settings` em `apps/backend/src/modules/settings` com a skill [backend-prisma-repository](../../../.claude/skills/backend-prisma-repository), sem alterar a interface do domínio.
  > ✅ 2026-07-04 — `settings.prisma.ts` (`PrismaStoreSettingsRepository`), registrado no `SettingsModule` com `DbModule`/`PrismaService`.
- [x] 2.3 Criar `apps/backend/src/modules/settings/settings.controller.ts` com a skill [backend-nest-controller](../../../.claude/skills/backend-nest-controller), expondo **apenas** `GET /settings` (`@Public()`, retorna o registro único ou um valor default se ainda não configurado) e `PUT /settings` (autenticado, instancia `save-store-settings` com `STORE_SETTINGS_ID`). Sem `POST`, `DELETE` nem listagem.
  > ✅ 2026-07-04 — `SettingsController` substitui o stub gerado pelo `config-new-module`. `GET /settings` (`@Public()`) retorna `{ whatsappNumber }`, `null` se não configurado. `PUT /settings` (204, autenticado) instancia `SaveStoreSettings` — `STORE_SETTINGS_ID` é resolvido dentro do usecase, o body só traz `whatsappNumber`.
- [x] 2.4 Registrar `SettingsModule` em `apps/backend/src/app.module.ts`.
  > ✅ 2026-07-04 — Já registrado automaticamente pelo script `config-new-module` na task 1.1.
- [x] 2.5 Criar `apps/backend/src/modules/settings/settings.integration.http` cobrindo: leitura sem configuração prévia, atualização válida, atualização com número inválido, leitura sem JWT (deve funcionar, é pública), atualização sem JWT (deve ser bloqueada com 401).
  > ✅ 2026-07-04 — Cenários criados. **Testado em runtime via curl** (backend rodando): leitura inicial `{"whatsappNumber":null}` (200), `PUT` sem JWT → 401, `PUT` com JWT e número válido → 204 e `GET` reflete o valor, segundo `PUT` atualiza o mesmo registro (não cria um segundo), `PUT` com número inválido → 422 com `settings.whatsappNumber.phone`. `npx tsc --noEmit` no backend ok.

## 3. Back-end — `catalog` (leitura pública da vitrine)

- [x] 3.1 Criar `apps/backend/src/modules/catalog/storefront.controller.ts` com a skill [backend-nest-controller](../../../.claude/skills/backend-nest-controller), expondo `GET /storefront/products` (`@Public()`), filtrando produtos com `status: 'active'` e incluindo, para cada um, a quantidade de estoque (via `StockRepository.findByProductId`). Sem paginação nesta primeira entrega (retorna todos os produtos ativos de uma vez), já que o volume esperado é pequeno.
  > ✅ 2026-07-04 — `StorefrontController` busca até 500 produtos via `productRepository.findPage` (sem alterar o contrato de domínio) e filtra `status === 'active'` no próprio controller; para cada um, resolve `quantity` via `stockRepository.findByProductId` (método já existente desde a change-006). Resposta: `{ id, name, description, price, images, categoryId, quantity }`.
- [x] 3.2 Registrar `StorefrontController` no `CatalogModule`.
  > ✅ 2026-07-04 — Adicionado à lista de `controllers` do `CatalogModule`, reaproveitando os providers `PrismaProductRepository`/`PrismaStockRepository` já registrados.
- [x] 3.3 Criar `apps/backend/src/modules/catalog/storefront.integration.http` cobrindo: listagem retorna só produtos `active`, produto com estoque zero aparece com `quantity: 0` (sem ser omitido), acesso sem JWT funciona (rota pública).
  > ✅ 2026-07-04 — Cenários criados. **Testado em runtime via curl**: criado um produto `active` e um `draft`; `GET /storefront/products` sem JWT retornou 200 com apenas os produtos ativos (o `draft` não apareceu), cada item com `quantity` resolvida corretamente. `npx tsc --noEmit` no backend ok.

## 4. Front-end — `settings` (admin)

- [x] 4.1 Criar a tela única de "Configurações" no módulo `settings`, em rota privada (`/settings` ou similar dentro de `(private)`), sem listagem/criar/excluir — só visualizar e editar o campo `whatsappNumber`.
  > ✅ 2026-07-04 — `settings.component.tsx` substitui o stub gerado pelo `config-new-module`. Carrega o valor atual via `getStoreSettings()` (`GET /settings`, sem autenticação necessária), formulário único com o campo `whatsappNumber`, salva via `updateStoreSettings()` (`PUT /settings`, autenticado — o token já vai automaticamente pois a rota está em `(private)`, atrás do `AuthGuard`). Sem botão de criar, sem lista, sem excluir. `settings-api.util.ts` criado com `getStoreSettings`/`updateStoreSettings`.
- [x] 4.2 Adicionar o item "Configurações" no menu lateral apontando para essa tela.
  > ✅ 2026-07-04 — Novo módulo de navegação `settings` (ícone `Settings`) em `APP_MODULES`, com item "Configurações" apontando para `/settings`.
  > ✅ 2026-07-04 (correção pós-conferência manual, bug pré-existente desde a change-006) — `AppSidebarNavigation` decidia qual módulo estava "ativo" comparando a rota atual só com `module.item.href` (a rota principal do módulo, ex. `/products`), ignorando as demais rotas do mesmo módulo (`/categories`, `/stock`, e agora `/settings`). Ao navegar para "Categorias" ou "Estoque", a barra lateral não reconhecia a página como parte do módulo "Catálogo" e caía no `defaultModuleId` ("Exemplo"), fazendo a navegação "voltar" visualmente para outro módulo mesmo com a página certa carregada. Corrigido em `app-sidebar-navigation.component.tsx`: a detecção de módulo ativo agora verifica todas as rotas de todas as seções do módulo, não só a rota principal.
- [x] 4.3 Acrescentar as chaves novas de i18n (validação de `whatsappNumber`), reaproveitando chaves existentes.
  > ✅ 2026-07-04 — `messages.pt.ts`/`messages.en.ts` com `settings.whatsappNumber.{required,phone}`. `npx tsc --noEmit` em `apps/frontend` ok.

## 5. Front-end — `storefront` (vitrine pública, carrinho, WhatsApp)

- [x] 5.1 Criar layout próprio para a rota pública `/loja` (fora de `(private)` e do `PublicBoxedLayout` de autenticação): largura total, grade responsiva (1–2 colunas no mobile, 2–3 no tablet, 3–4 no desktop).
  > ✅ 2026-07-04 — `app/loja/page.tsx` (fora de qualquer grupo de rotas) renderiza `StorefrontPage`, que já contém seu próprio header full-width — sem `layout.tsx` dedicado, já que só existe uma rota nesta entrega (evitou indireção desnecessária). Grade em `product-grid.component.tsx`: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`.
- [x] 5.2 Criar o card de produto: imagem de capa (primeira imagem de `images`, se houver, com um estado vazio caso não haja imagem), nome, descrição truncada em 2 linhas (`line-clamp-2`), preço formatado, botão "Adicionar ao carrinho". Produto com `quantity: 0` mostra badge "Esgotado" e botão desabilitado.
  > ✅ 2026-07-04 — `product-card.component.tsx`: capa (`images[0]`) ou placeholder com ícone `ImageOff`; nome; descrição com `line-clamp-2`; preço via `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`; badge "Esgotado" (componente `Badge` já existente) e botão desabilitado quando `quantity <= 0`.
  > ✅ 2026-07-04 (ajuste pós-conferência manual) — Adicionada ampliação de foto: clicar na imagem de capa abre `product-image-viewer.component.tsx` (tela cheia, fundo escuro, imagem em `object-contain`) com botão explícito "Voltar" (além do fechar padrão do `Dialog`, ocultado nesta variante via `[&>button]:hidden` para não duplicar o controle de fechar).
  > ✅ 2026-07-04 (redesenho pós-conferência manual, a partir de wireframe fornecido pelo usuário) — Card reestruturado de layout vertical (imagem em cima, ocupando a largura toda) para horizontal: imagem quadrada fixa (`size-28`, cantos arredondados) à esquerda, informações à direita (nome, preço, botão "Adicionar" em coluna). **Desvio consciente do requisito original**: a descrição do produto (antes truncada em 2 linhas) foi removida da exibição do card para caber no layout horizontal mais compacto, seguindo o wireframe (que só mostra nome, preço e botão) — a descrição completa continua disponível via ampliação da imagem/detalhe do produto se uma tela de detalhe for adicionada no futuro.
  > ✅ 2026-07-04 (correção) — Radix acusava erro de acessibilidade (`DialogContent requires a DialogTitle`) no visualizador de imagem, que não tinha título. Adicionado `DialogTitle` visível (nome do produto) ao lado do botão "Voltar".
  > ✅ 2026-07-04 (ajuste pós-conferência manual) — Adicionada miniatura da foto do produto em cada linha do carrinho. `CartItem` ganhou o campo `image: string | null`; `addItem` passa a receber a imagem de capa (`product.images[0] ?? null`); `cart-sheet.component.tsx` renderiza a miniatura (ou ícone `ImageOff` como placeholder). Itens de carrinho salvos no `localStorage` antes deste ajuste continuam funcionando — sem `image`, exibem o placeholder normalmente.
  > ✅ 2026-07-04 (ajuste pós-conferência manual) — Layout da linha do carrinho reorganizado em duas linhas por item: nome do produto (até 2 linhas, sem truncar agressivamente) com o botão de excluir ao lado, e preço + stepper de quantidade numa segunda linha. Antes, nome/preço/stepper/excluir disputavam a mesma linha estreita no mobile, truncando o nome de forma ilegível. O stepper de quantidade ganhou um contorno (pill) para se destacar como um controle agrupado, separado da ação destrutiva de excluir.
  > ✅ 2026-07-04 (ajuste de espaçamento pós-conferência manual) — Trocado `justify-between` (que espalhava nome/excluir e preço/stepper nas duas pontas da linha, criando vãos grandes numa `Sheet` agora full-width no mobile) por agrupamento à esquerda com `gap` fixo — excluir fica logo ao lado do nome, stepper logo ao lado do preço, sobrando espaço em branco só no final da linha (mais natural). Reduzidos também: espaço entre itens do carrinho (`gap-4`→`gap-3`), padding inferior de cada item (`pb-4`→`pb-3`), gap interno entre as duas linhas do item (`gap-2`→`gap-1.5`) e tamanho dos botões do stepper (`size-8`→`size-7`).
  > ✅ 2026-07-04 (correção pós-conferência manual) — `SheetContent` (componente compartilhado, usado pelo carrinho e pelo menu mobile do admin) estava sem `overflow-x-hidden`, deixando aparecer uma barra de rolagem horizontal indesejada no painel do carrinho. Adicionado `overflow-x-hidden` na classe base do `SheetContent` — corrige o carrinho e previne o mesmo problema em qualquer outro uso futuro do `Sheet`.
  > ✅ 2026-07-04 (correção pós-conferência manual) — O `overflow-x-hidden` no `SheetContent` não resolveu sozinho — a rolagem horizontal era da página por trás do painel (`html`/`body`), não do próprio `Sheet` (um elemento `fixed` na página pode continuar causando barra de rolagem horizontal do documento mesmo com o painel aberto). Adicionado `overflow-x: hidden` em `html, body` no `globals.css` — fix de nível de app, elimina rolagem horizontal indesejada em qualquer página, não só na vitrine.
  > ✅ 2026-07-04 (causa real encontrada e corrigida) — A barra de rolagem horizontal persistia mesmo após as duas correções acima porque o elemento com o scroll de verdade era o `<div className="... overflow-y-auto">` (lista de itens do carrinho) em `cart-sheet.component.tsx`: por uma particularidade da spec de CSS, definir só `overflow-y` sem `overflow-x` explícito faz o navegador tratar o eixo X como `auto` (rolável) também. Corrigido acrescentando `overflow-x-hidden` nesse mesmo elemento — cada contêiner com scroll próprio precisa declarar os dois eixos explicitamente, não basta corrigir só o `SheetContent` pai.
  > ✅ 2026-07-04 (correção pós-conferência manual) — O botão de excluir do item do carrinho usava margens negativas (`-mr-2 -mt-2`) para encostar visualmente no canto da linha; isso deixava a área clicável/o retângulo de hover do botão visualmente deslocado/cortado. Removidas as margens negativas — o botão agora fica dentro do fluxo normal da linha, sem cortes.
  > ✅ 2026-07-04 (ajuste pós-conferência manual) — A linha nome+excluir usava `items-start` (alinhamento pelo topo), deixando o botão de excluir desalinhado verticalmente em relação ao texto do nome. Trocado para `items-center`.
  > ✅ 2026-07-04 (ajuste pós-conferência manual) — Removido o `gap-3` (12px) entre o preço e o stepper de quantidade, que deixava os dois muito afastados; trocado por `ml-2` (8px) só no stepper, mantendo os dois agrupados à esquerda com um respiro mínimo.
  > ✅ 2026-07-04 (ajuste pós-conferência manual) — Reduzido o gap vertical entre a linha do nome e a linha de preço/quantidade (o container que empilha as duas), de `gap-1.5` para `gap-0.5` — as duas linhas ficam visualmente mais coladas, como um bloco só.
  > ✅ 2026-07-04 (ajuste pós-conferência manual) — Estoque: removido o botão "Novo estoque" da listagem (`stock-list.component.tsx`) e, por consequência, a página/rota `/stock/new` (ficaria órfã, inalcançável sem o botão) e toda a lógica de criação em `stock-form.component.tsx` (select de produto sem estoque, `createStock`, `listProductsWithoutStock`, tipo `ProductOption` — removidos de `stock-api.util.ts` por não terem mais consumidor). `StockForm` agora só edita (prop `initialStock` obrigatória, sem mais `mode`). O texto do estado vazio da listagem foi ajustado para explicar que o estoque nasce automaticamente ao cadastrar o produto. **Isso reduz o escopo da Decisão 3 do design.md (change-006)** — o backend continua expondo `POST /stock` (não removido, é inofensivo manter), mas o frontend não oferece mais criação manual.
  > ✅ 2026-07-04 (ajuste pós-conferência manual) — Linha de preço + stepper de quantidade do carrinho: trocado agrupamento à esquerda por `justify-between`, alinhando o stepper ao fim da linha (à direita), com o preço à esquerda.
- [x] 5.3 Implementar o carrinho (React Context) persistido em `localStorage`: adicionar item, alterar quantidade, remover item, calcular total. Deve sobreviver a reload da página.
  > ✅ 2026-07-04 — `cart.context.tsx` (`CartProvider`/`useCart`), chave `storefront.cart` no `localStorage`. Hidrata do `localStorage` no mount (guardado contra SSR com checagem de `typeof window`) e persiste a cada mudança. `addItem` incrementa quantidade se o produto já está no carrinho; `updateQuantity` remove o item se a quantidade cair a zero ou menos.
  > ✅ 2026-07-04 (ajustes pós-conferência manual) — Três correções apontadas pelo usuário ao testar de verdade: (1) a ilustração de "lista vazia" (`empty-list-state.tsx`, componente compartilhado usado também por produtos/categorias/estoque) referenciava `/illustrations/empty-list-dark.svg`, um arquivo que nunca existiu no projeto — criado o SVG faltante em `apps/frontend/public/illustrations/`, corrigindo o carrinho vazio e todo estado vazio do app. (2) O componente compartilhado `Sheet` (usado pelo carrinho e pelo menu mobile do admin) não tinha botão de fechar — adicionado um `SheetClose` com ícone `X`, mesmo padrão do `Dialog`. (3) Em telas abaixo do breakpoint `sm` (inclui 320px), o `Sheet` ocupava só `w-3/4` da largura — trocado para `w-full` no mobile e `sm:w-3/4 sm:max-w-sm` a partir do breakpoint `sm`, preenchendo a tela toda em telas pequenas sem afetar o tamanho já customizado do menu mobile do admin (que sobrescreve a largura via `className`, resolvido corretamente pelo `twMerge`).
- [x] 5.4 Criar a etapa de fechamento do pedido: exibe o resumo do carrinho, captura o nome do cliente (campo obrigatório), monta a mensagem de texto (nome, itens com quantidade/preço, total geral) e abre `https://wa.me/<whatsappNumber sem o "+">?text=<mensagem codificada com encodeURIComponent>`, buscando `whatsappNumber` via `GET /settings`.
  > ✅ 2026-07-04 — `cart-sheet.component.tsx` (painel lateral com itens, controles de quantidade, remoção e total) → botão "Finalizar pedido" abre `checkout-dialog.component.tsx`, que valida o nome do cliente, busca `getStoreSettings()`, monta a mensagem via `buildWhatsappMessage` e o link via `buildWhatsappLink` (`whatsapp-message.util.ts`) e abre o link numa nova aba. Se a loja ainda não configurou o número, mostra erro em vez de abrir um link quebrado. Ao enviar com sucesso, o carrinho é limpo.
- [x] 5.5 Acrescentar as chaves novas de i18n necessárias (ex.: rótulo "Esgotado", mensagens de validação do nome do cliente), reaproveitando chaves existentes.
  > ✅ 2026-07-04 — `storefront.customerName.required` e `storefront.whatsappNumber.not_configured` em `messages.pt.ts`/`messages.en.ts`. O rótulo "Esgotado" é texto fixo da UI (não é uma mensagem de erro vinda do backend), então não precisou de chave de i18n dedicada — mesmo padrão de outros textos estáticos de UI no projeto.
- [x] 5.6 Rodar `npx tsc --noEmit` em `apps/frontend` e sinalizar ao usuário que a UI está pronta para conferência manual.
  > ✅ 2026-07-04 — `npx tsc --noEmit` em `apps/frontend` sem erros. **Testado em runtime** (servidor de desenvolvimento já em execução): `GET /loja` e `GET /settings` retornam 200; o HTML de `/loja` contém o header "Loja" e o estado inicial "Carregando produtos" (a busca de produtos acontece client-side); `GET /storefront/products` no backend continua respondendo no formato esperado pelo frontend. **UI pronta para conferência manual do usuário** — em particular, o fluxo de carrinho → nome do cliente → abrir o WhatsApp com a mensagem correta só pode ser validado interagindo de verdade no navegador (não há verificação automatizada de UI nesta change, decisão registrada no design.md).

## 6. Ajustes pós-entrega solicitados pelo usuário (fora do escopo original desta change)

- [x] 6.1 Permitir upload de imagem no formulário de **criação** de produto (antes só era possível em edição, por decisão da change-006 — `POST /products/:id/images` exige um produto já existente).
  > ✅ 2026-07-04 — **Mudança de backend** (estende `catalog-product-backend` da change-006): `POST /products` agora aceita opcionalmente `id` no corpo (`body.id ?? randomUUID()`) e passa a **retornar `{ id }`** no corpo da resposta (antes era `201` sem corpo) — necessário para o frontend saber contra qual produto anexar as imagens logo após criar. Testado via curl: criar produto devolve `{ id }`, upload de imagem contra esse `id` funciona (`201`, imagem refletida no `GET /products/:id`). **Frontend** (`product-form.component.tsx`): seção "Imagens" agora aparece também em modo criação; arquivos selecionados ficam em estado local (`pendingFiles`, com preview via `URL.createObjectURL`, revogado no cleanup do `useEffect`) e só são enviados ao backend (`uploadProductImage`) depois que `createProduct` retorna o `id` do produto recém-criado. Falha ao enviar uma imagem individual não impede a criação do produto (já concluída) — mostra um toast de erro por arquivo que falhar. `npx tsc --noEmit` ok; testes do backend (35, `@sdd/catalog`) e build do backend permanecem verdes.
- [x] 6.2 Na vitrine pública, se o produto tiver mais de uma imagem, permitir visualizar todas via slider.
  > ✅ 2026-07-04 — `product-image-viewer.component.tsx` (change-007) agora recebe a lista completa `images: string[]` (antes só a capa) e um `initialIndex`. Com mais de uma imagem: setas de navegação (anterior/próxima, com wrap-around), contador "N / total" no cabeçalho e indicadores (bolinhas) clicáveis na parte inferior. `product-card.component.tsx` passa `product.images` inteiro para o visualizador e ganhou um selo com ícone + contagem de imagens no canto da miniatura quando há mais de uma, sinalizando a galeria antes mesmo de abrir. `npx tsc --noEmit` ok.
- [x] 6.3 Refatorar a seção "Disponibilidade" do formulário de produto (`availableOnline`/`featured`/`allowsPreOrder`) para os flags de marketing "Mais vendido"/"Oferta do dia"/"Últimas unidades", e exibir um badge correspondente no card da vitrine (exemplo visual fornecido pelo usuário: badge laranja "MAIS VENDIDO").
  > ✅ 2026-07-04 — **Renomeação de campos existentes desde a change-005/006** (`Product.availableOnline`→`bestSeller`, `featured`→`dailyDeal`, `allowsPreOrder`→`lastUnits`), mesmo significado estrutural (booleanos independentes, default `false`), só o nome/propósito muda. Tocou: entidade (`product.entity.ts`), `SaveProductIn`, testes do usecase (`save-product.usecase.test.ts`), model Prisma + **migration manual** (`20260704210140_catalog_product_marketing_flags` — `ALTER TABLE ... RENAME COLUMN`, preservando os dados existentes em vez do drop+recreate que o `prisma migrate dev` sugeriu interativamente, indisponível neste ambiente não-interativo), `product.prisma.ts`, `product.controller.ts` (tipo de resposta + mapeamento), `storefront.controller.ts` (novos campos na resposta pública), `products-api.util.ts`, `product-form.component.tsx` (checkboxes renomeados: "Mais vendido", "Oferta do dia", "Últimas unidades"), `storefront-api.util.ts` e `product-card.component.tsx` (badges: "Oferta do dia" em vermelho, "Mais vendido" em laranja, "Últimas unidades" em âmbar, empilhados no canto superior esquerdo da miniatura, acima/junto do badge "Esgotado" quando aplicável). **Testado em runtime via curl**: criar produto com `bestSeller`/`dailyDeal` reflete corretamente em `GET /products/:id` e em `GET /storefront/products`. 35 testes do `@sdd/catalog` e build do backend permanecem verdes; `npx tsc --noEmit` limpo nos dois apps.
- [x] 6.4 Ajustes visuais do card de produto da vitrine, pedidos pelo usuário a partir de prints reais renderizados (badges sobrepostos, espaçamento desigual, cantos arredondados inconsistentes, botão/preço chamando atenção demais).
  > ✅ 2026-07-04 — Vários ajustes incrementais em `product-card.component.tsx` (e, por consistência, `cart-sheet.component.tsx`): (1) badges de marketing + "Esgotado" empilhados sobre a miniatura causavam sobreposição — trocado para exibir **só um selo por vez** (`getPrimaryBadge`, prioridade Esgotado > Oferta do dia > Últimas unidades > Mais vendido), com estilo mais discreto (chip translúcido tonal em vez de fundo sólido saturado); (2) badge movido de sobre a imagem para acima do título (dentro da coluna de texto); (3) hierarquia da informação invertida — título virou secundário (`text-muted-foreground`, `font-medium`) e o preço virou o elemento de destaque (`text-xl font-bold`); (4) cantos arredondados padronizados entre `Card` (`rounded-xl`), miniatura da imagem e botão (`rounded-lg`), incluindo a miniatura do carrinho; (5) espaçamento da coluna de texto trocado de `justify-between` (gerava vãos desiguais) para margens individuais + botão fixado na base via `mt-auto`; (6) cor do preço trocada de `text-primary` (âmbar) para `text-foreground`; (7) botão "Adicionar" trocado de preenchimento sólido verde vibrante para estilo discreto (`bg-emerald-600/10`, borda fina, texto `text-emerald-500`); (8) scrollbar grossa do carrinho trocada por `.thin-scrollbar` (classe utilitária nova em `globals.css`, 6px translúcida). Sem verificação automatizada de UI (decisão do design.md) — todos os ajustes conferidos por `npx tsc --noEmit` limpo e pelo usuário visualmente a cada rodada.
- [x] 6.5 Adicionar campo de quantidade em estoque diretamente no formulário de produto, evitando a necessidade de abrir a tela separada de Estoque só para definir a quantidade inicial ou ajustá-la.
  > ✅ 2026-07-04 — **Backend**: `POST /products` passa a aceitar `quantity` opcional no corpo (usado como quantidade inicial do estoque criado automaticamente, default `0` quando ausente); `PUT /products/:id` passa a aceitar `quantity` opcional — quando informado, localiza o estoque existente via `StockRepository.findByProductId` e atualiza sua quantidade via `SaveStock` (nunca cria um segundo registro); `GET /products/:id` e a listagem passam a incluir `quantity`, resolvida do estoque associado (mesmo padrão de enriquecimento já usado pelo `StockController` para `productName`). **Frontend**: campo "Quantidade em estoque" na seção "Preço e status" do `product-form.component.tsx`. **Testado via curl**: criar produto com `quantity: 25` → `GET /products/:id` reflete `25`; `PUT` com `quantity: 40` atualiza o mesmo registro de estoque (confirmado também em `GET /stock`, sem duplicidade); `npx tsc --noEmit` e build do backend limpos.
