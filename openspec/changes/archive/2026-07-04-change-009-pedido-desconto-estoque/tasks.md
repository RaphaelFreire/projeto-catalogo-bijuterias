## Instruções Compartilhadas

Estas instruções valem para qualquer change deste projeto e devem ser respeitadas durante a execução das tasks abaixo:

- [Como executar](../../shared/como-executar.md) — regras de execução e formato de evidência por task.
- [Regras de nomenclatura](../../shared/regras-de-nomenclatura.md) — convenções de nomes de arquivos e diretórios.

## 1. Negócio — estender `StockRepository` com decremento atômico

- [x] 1.1 Adicionar `decrementIfAvailable(productId: string, quantity: number): Promise<boolean>` à interface `StockRepository` (`modules/catalog/src/stock/provider/stock.repository.ts`). Documentar no próprio tipo que a implementação técnica SHALL ser atômica (sem ler-depois-escrever).
  > ✅ 2026-07-04 — Método adicionado com JSDoc explicando a exigência de atomicidade (update condicional no banco, nunca ler-depois-escrever).
- [x] 1.2 Atualizar `FakeStockRepository` (`modules/catalog/test/mock`) com uma implementação simples de `decrementIfAvailable` (checa e decrementa em memória — atomicidade não se aplica a fakes de teste).
  > ✅ 2026-07-04 — Implementado em memória: busca o stock por `productId`, retorna `false` se não existe ou `quantity < quantity` pedida, senão decrementa via `clone()`.

## 2. Negócio — agregado `order` (módulo catalog)

- [x] 2.1 Criar o agregado `order` dentro do módulo `catalog` com a skill [module-aggregate](../../../.claude/skills/module-aggregate).
  > ✅ 2026-07-04 — Scaffolding gerado com `--mode example` (só `create-order`, sem CRUD completo — coerente com a Decisão 2 do design). Adicionado manualmente um `error/order-not-found.error.ts` (`OrderNotFoundError`, mesmo padrão de `BannerNotFoundError`), usado pelos controllers para `GET /orders/:id` e `GET /storefront/orders/:code`.
- [x] 2.2 Implementar a entidade `Order` com a skill [module-entity](../../../.claude/skills/module-entity), com os campos: `code` (string, required, gerado externamente antes da instanciação — a entidade só valida o formato), `customerName` (string, required, min-length 2), `items` (array required, min-items 1, cada item com `productId` uuid, `name` string, `price` number min-value 0, `quantity` integer min-value 1), `total` (number, required, min-value 0).
  > ✅ 2026-07-04 — `code` validado com `RegexRule(/^[A-Z0-9]{8}$/)` além de `RequiredRule`. Validação de cada item de `items` feita manualmente (reaproveitando instâncias de `UuidRule`/`RequiredRule`/`IntegerRule`/`MinValueRule` e agregando os erros num único `ValidationException`), já que não existe uma regra pronta no `@sdd/shared` para validar array de objetos campo-a-campo.
- [x] 2.3 Definir o contrato do repositório de `order` com a skill [module-repository](../../../.claude/skills/module-repository), no formato `CrudRepository` padrão, mais `findByCode(code: string): Promise<Order | null>`.
  > ✅ 2026-07-04 — `OrderRepository extends CrudRepository<Order, Order, Order, OrderPageParams>` mais `findByCode`. `update`/`delete` existem no contrato (uniformidade com os demais agregados), mas nunca são chamados por nenhum caso de uso/controller — pedido é imutável na prática (Decisão 2 do design).
- [x] 2.4 Implementar o caso de uso `create-order` com a skill [module-use-case](../../../.claude/skills/module-use-case): recebe `customerName` e `items` (com `productId`/`name`/`price`/`quantity` já resolvidos pelo chamador — o caso de uso não busca produto, só monta e persiste a entidade), calcula `total` a partir dos itens, gera um `code` curto (ex.: 8 caracteres alfanuméricos maiúsculos) verificando unicidade via `findByCode` antes de persistir (gera outro em caso de colisão, até um número pequeno de tentativas), e retorna o `code` gerado. **Sem caso de uso de atualização ou exclusão** — pedido é imutável após criado (decisão registrada no design.md).
  > ✅ 2026-07-04 — `CreateOrder.execute` retorna `Promise<string>` (o `code`). `generateUniqueCode` tenta até `MAX_CODE_ATTEMPTS = 5` vezes; se todas colidirem (praticamente impossível com espaço de 36⁸ códigos), lança `DomainError("order.code.generation_failed", 500)`.
- [x] 2.5 Cobrir o caso de uso com testes unitários, usando `FakeOrderRepository`. Cenários mínimos: criação com itens válidos calcula o `total` corretamente, geração de `code` único, nova tentativa de geração em caso de colisão simulada, validações principais (`customerName` curto, `items` vazio, item com `quantity` menor que 1).
  > ✅ 2026-07-04 — 8 testes em `create-order.usecase.test.ts`, incluindo colisão simulada via `jest.spyOn(repo, "findByCode")` forçando a primeira chamada a retornar um pedido existente. `npm run test --workspace @sdd/catalog` → 57 testes passando (100% cobertura em `order.entity.ts`/`create-order.usecase.ts`, exceto os 2 branches defensivos: guarda `!Array.isArray(items)` e o `throw` de colisão esgotada).

## 3. Back-end — `stock` (decremento atômico)

- [x] 3.1 Implementar `decrementIfAvailable` em `PrismaStockRepository` (`apps/backend/src/modules/catalog/stock.prisma.ts`) usando `this.prisma.stock.updateMany({ where: { productId, quantity: { gte: quantity } }, data: { quantity: { decrement: quantity } } })`, retornando `true` se `result.count > 0`, `false` caso contrário.
  > ✅ 2026-07-04 — Implementado exatamente como especificado.
- [x] 3.2 Atualizar `stock.integration.http` com um cenário manual de decremento bem-sucedido e um de decremento rejeitado por estoque insuficiente (pode ser testado indiretamente via `POST /storefront/checkout`, ver task 4.6).
  > ✅ 2026-07-04 — Nota adicionada ao final de `stock.integration.http` apontando para os cenários equivalentes em `order.integration.http` (checkout bem-sucedido e rejeitado). Confirmado em runtime via smoke test (ver evidência da task 4.6).

## 4. Back-end — `order`

- [x] 4.1 Sincronizar o módulo `catalog` com o Prisma criando o model `Order` com a skill [backend-prisma-sync-module](../../../.claude/skills/backend-prisma-sync-module). Campos: `id`, `code` (`@unique`), `customerName`, `items` (tipo `Json`), `total` (`Decimal`), `createdAt`. Sem `updatedAt`/`deletedAt` — pedido não é atualizado nem excluído.
  > ✅ 2026-07-04 — Model `Order` (`@@map("orders")`) em `catalog.model.prisma`, exatamente com os campos listados. Migration `20260705014308_catalog_stock_decrement_order` gerada via `npx prisma migrate dev --name catalog-stock-decrement-order` e aplicada (inclui também o `Banner.categoryId`/relação opcional já existente da change-008, sem alteração nova aqui).
- [x] 4.2 Implementar o repositório Prisma de `order` em `apps/backend/src/modules/catalog` com a skill [backend-prisma-repository](../../../.claude/skills/backend-prisma-repository), incluindo `findByCode`, sem alterar a interface do domínio além do que já foi definido nela.
  > ✅ 2026-07-04 — `order.prisma.ts` (`PrismaOrderRepository`). `total` mapeado com `new Prisma.Decimal(...)`/`.toNumber()`, mesmo padrão de `Product.price`. `update`/`delete` implementados por completude de interface, mas nunca chamados (ver evidência da task 2.3).
- [x] 4.3 Criar `POST /storefront/checkout` (`@Public()`) no `StorefrontController` já existente: recebe `{ customerName, items: [{ productId, quantity }] }`. Dentro de uma transação Prisma interativa (`$transaction(async (tx) => ...)`): para cada item, busca o produto (nome/preço atuais) e chama `decrementIfAvailable`; se qualquer um retornar `false`, aborta a transação e responde `422` com `{ errors: ["order.stock.insufficient"], insufficientItems: [{ productId }] }`. Se todos os itens tiverem estoque suficiente, monta os itens do pedido com nome/preço **daquele momento**, instancia `CreateOrder` e persiste o pedido dentro da mesma transação. Retorna `201` com `{ code }`.
  > ✅ 2026-07-04 — Dentro do `$transaction`, `tx` (tipo `Prisma.TransactionClient`) é convertido via `as unknown as PrismaService` para reaproveitar `PrismaStockRepository`/`PrismaOrderRepository` sem duplicar lógica de acesso a dados — comentário no código explica que o cast é seguro porque os repositórios só usam os delegates de modelo, presentes em ambos os tipos. Produto inexistente é tratado como item insuficiente (mesma lista `insufficientItems`). O corpo de erro usa `HttpException({ message: ['order.stock.insufficient'], insufficientItems }, 422)` — o `ApiExceptionFilter` (não alterado) já extrai `errors` de `res.message` quando é array, e expõe o objeto completo (incluindo `insufficientItems`) em `details`, sem precisar modificar o filtro compartilhado.
- [x] 4.4 Criar `GET /storefront/orders/:code` (`@Public()`) no `StorefrontController`, retornando `{ code, customerName, items, total, createdAt }` ou `404` (`order.not_found`) se o código não existir.
  > ✅ 2026-07-04 — Implementado.
- [x] 4.5 Criar `apps/backend/src/modules/catalog/order.controller.ts` com a skill [backend-nest-controller](../../../.claude/skills/backend-nest-controller), autenticado, expondo **apenas** `GET /orders` (paginado) e `GET /orders/:id` — sem `POST`/`PUT`/`DELETE`. Registrar no `CatalogModule`.
  > ✅ 2026-07-04 — `OrderController` registrado em `CatalogModule` (controllers/providers/exports), junto com `PrismaOrderRepository`.
- [x] 4.6 Criar/atualizar `apps/backend/src/modules/catalog/storefront.integration.http` e um novo `order.integration.http` cobrindo: checkout bem-sucedido (verificando que o estoque foi descontado e o pedido criado com `code`), checkout rejeitado por estoque insuficiente (422, verificando que **nada** foi descontado nesse caso), consulta pública por código existente e inexistente (404), listagem paginada de pedidos no admin (autenticado), obter pedido por id no admin, acesso sem JWT em `/orders` (401), acesso sem JWT em `/storefront/checkout` e `/storefront/orders/:code` (deve funcionar, são públicos).
  > ✅ 2026-07-04 — `order.integration.http` criado com todos os cenários. **Testado em runtime via curl** contra uma instância isolada do backend (porta 4002, sem afetar processos do usuário): produto criado com `quantity: 5`; checkout de 2 unidades → `201 { code }`, estoque cai para 3; consulta pública do código retorna o snapshot correto (`name`/`price` no momento da compra); código inexistente → `404 order.not_found`; checkout pedindo 100 unidades → `422` com `errors: ["order.stock.insufficient"]` e `details.insufficientItems`, **e o estoque continuou em 3** (rollback da transação confirmado); checkout com produto inexistente → mesmo erro 422; listagem `GET /orders` (admin) reflete o pedido criado; `GET /orders/:id` retorna o mesmo pedido; `GET /orders/:id` inexistente → `404 order.not_found`; `GET /orders` sem JWT → `401`. `npx tsc --noEmit` no backend ok.

## 5. Front-end — admin "Pedidos"

- [x] 5.1 Criar `orders-api.util.ts` (`listOrders`, `getOrder`) no módulo `catalog`.
  > ✅ 2026-07-04 — Mesmo padrão de `stock-api.util.ts` (fetch com `authHeaders()`, `OrdersApiError`).
- [x] 5.2 Criar a listagem paginada de pedidos em rota privada: colunas código, nome do cliente, total, data. Sem ações de criar/editar/excluir (somente leitura).
  > ✅ 2026-07-04 — `orders-list.component.tsx` + rota `/orders`. Só ação "ver" (ícone `Eye`) navegando para o detalhe — sem editar/excluir, consistente com a Decisão 7 do design (admin é somente leitura).
- [x] 5.3 Criar a tela de detalhe do pedido (rota `/orders/[id]` ou equivalente): lista de itens (nome, quantidade, preço no momento da compra), total, nome do cliente, data, código.
  > ✅ 2026-07-04 — `order-detail.component.tsx` + rota `/orders/[id]`, tabela com nome/preço/quantidade/subtotal por item, total ao final.
- [x] 5.4 Adicionar o item "Pedidos" no menu lateral apontando para a listagem.
  > ✅ 2026-07-04 — Item `catalog-orders` (ícone `ClipboardList`) na seção `catalog-main`, junto com produtos/categorias/estoque.
- [x] 5.5 Acrescentar as chaves novas de i18n (`order.not_found`), reaproveitando chaves existentes.
  > ✅ 2026-07-04 — `order.not_found` e `order.stock.insufficient` (usada também pelo checkout, task 6.4) adicionadas em `messages.pt.ts`/`messages.en.ts`. `npx tsc --noEmit` em `apps/frontend` ok.

## 6. Front-end — `storefront` (checkout chama o backend, consulta pública)

- [x] 6.1 Atualizar `checkout-dialog.component.tsx`: ao confirmar, chamar `POST /storefront/checkout` com `customerName` e os itens do carrinho **antes** de montar a mensagem de WhatsApp. Se a resposta for `422` com itens de estoque insuficiente, exibir mensagem indicando quais produtos do carrinho não têm estoque suficiente (cruzando `insufficientItems` com os dados já presentes no carrinho) — o carrinho NÃO PODE ser limpo nem o WhatsApp aberto nesse caso.
  > ✅ 2026-07-04 — `checkout()` (novo, em `storefront-api.util.ts`) chamado antes de montar a mensagem. `CheckoutApiError` carrega `errors` e `insufficientItems`; o dialog cruza `insufficientItems` com `cart.items` para listar os nomes dos produtos sem estoque no toast de erro. Em qualquer erro, `cart.clear()`/`onOpenChange(false)` não são chamados — o `catch` não passa por eles.
- [x] 6.2 Se o checkout for bem-sucedido (retorna `{ code }`), incluir na mensagem de WhatsApp uma linha com o código do pedido e o link de consulta (`${window.location.origin}/pedido/${code}`), antes de abrir `wa.me` e limpar o carrinho.
  > ✅ 2026-07-04 — `buildWhatsappMessage` ganhou um 4º parâmetro opcional `{ code, link }`, acrescentando duas linhas ao final da mensagem quando informado.
- [x] 6.3 Criar a rota pública `/pedido/[codigo]` (fora do grupo `(private)`), buscando `GET /storefront/orders/:codigo` e exibindo o resumo do pedido (itens, total, nome do cliente, data). Código inexistente exibe um estado de "pedido não encontrado", sem erro de aplicação.
  > ✅ 2026-07-04 — `apps/frontend/src/app/pedido/[codigo]/page.tsx` (mesmo padrão top-level de `/loja`, fora de `(private)`/`(public)`), delegando a `order-lookup.page.tsx`. Qualquer falha na busca (404 ou erro de rede) cai no estado "Pedido não encontrado" — sem toast, sem stack trace, só a mensagem amigável.
  > ✅ 2026-07-04 — **`npm run build` (produção) executado com sucesso** — `/pedido/[codigo]` aparece como rota dinâmica (`ƒ`) gerada corretamente, sem erro de `useSearchParams`/Suspense (esta página não usa `useSearchParams`).
- [x] 6.4 Acrescentar as chaves novas de i18n necessárias (mensagem de estoque insuficiente no checkout, "pedido não encontrado"), reaproveitando chaves existentes.
  > ✅ 2026-07-04 — `order.stock.insufficient` usada no toast do checkout; "Pedido não encontrado" na página pública é texto estático de UI (mesma convenção já registrada na change-008 — não passa por i18n).
- [x] 6.5 Rodar `npx tsc --noEmit` em `apps/frontend` e sinalizar ao usuário que a UI está pronta para conferência manual.
  > ✅ 2026-07-04 — `npx tsc --noEmit` ok. `npm run build` (produção) ok, 23 rotas geradas. **UI pronta para conferência manual do usuário** — checkout chamando o backend antes do WhatsApp, mensagem com código/link do pedido, página pública `/pedido/[codigo]`, tela admin "Pedidos" (listagem + detalhe). O fluxo de backend (checkout, desconto atômico, rollback em falha, consulta pública, admin) foi validado via curl contra uma instância isolada do backend; a interação da UI em si (cliques, formulários) **não foi testada em navegador** por não haver ferramenta de automação de browser disponível neste ambiente — recomenda-se conferência manual antes de considerar esta change encerrada.
