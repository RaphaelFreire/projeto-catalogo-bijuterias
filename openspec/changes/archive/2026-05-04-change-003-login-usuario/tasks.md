## Instruções Compartilhadas

Estas instruções valem para qualquer change deste projeto e devem ser respeitadas durante a execução das tasks abaixo:

- [Como executar](../../shared/como-executar.md) — regras de execução e formato de evidência por task.
- [Regras de nomenclatura](../../shared/regras-de-nomenclatura.md) — convenções de nomes de arquivos e diretórios.

## 1. Negócio (módulo auth)

- [x] 1.1 Implementar o caso de uso `login-user` com a skill [module-use-case](../../../.claude/skills/module-use-case). Entrada: `{ email, password }`. Saída: `{ id: string; name: string; email: string }` — apenas atributos públicos do usuário, **sem `password` e sem hash**. Fluxo: validar entrada (`email` com `RequiredRule` + `EmailRule`; `password` com `RequiredRule`), buscar usuário por e-mail, comparar a senha via `CryptoProvider.comparePassword`. Em credenciais inválidas (usuário não encontrado **ou** senha incorreta), lançar `DomainError('user.credentials.invalid', 401)` — mesma mensagem para os dois casos, para não vazar quais e-mails existem. O caso de uso **não conhece nem menciona token/JWT**.
  > ✅ 2026-05-04 19:30 — Criado `modules/auth/src/user/usecase/login-user.usecase.ts` (`LoginUser`) com saída `{ id, name, email }` (sem `password`/hash) e novo `InvalidCredentialsError` em `modules/auth/src/user/error/invalid-credentials.error.ts` (`DomainError('user.credentials.invalid', 401)`). Validação aplica `RequiredRule + EmailRule` em `email` e `RequiredRule` em `password` antes de qualquer acesso ao repositório. Mesma exceção é lançada para usuário não encontrado e senha incorreta — não vaza existência de e-mails. Desvio: a interface `CryptoProvider` existente expõe `compare(plain, hash)` (não `comparePassword`); usei o método existente para preservar o contrato vigente. Domínio segue 100% sem qualquer referência a JWT/token/sessão. Exports atualizados em `error/index.ts` e `usecase/index.ts`.
- [x] 1.2 Cobrir o caso de uso com testes unitários reaproveitando os fakes existentes (`FakeUserRepository`, `FakeCryptoProvider`). Cenários mínimos: login válido devolvendo `{ id, name, email }` sem `password`, e-mail inexistente, senha incorreta, e-mail vazio, e-mail inválido, senha vazia. Coverage 100% no caso de uso.
  > ✅ 2026-05-04 19:34 — `modules/auth/test/user/usecase/login-user.usecase.test.ts` cobre 6 cenários (login válido, e-mail inexistente, senha incorreta, e-mail vazio, e-mail inválido, senha vazia) reaproveitando `FakeUserRepository`/`FakeCryptoProvider`. `npx jest test/user/usecase/login-user.usecase.test.ts --coverage --collectCoverageFrom='src/user/usecase/login-user.usecase.ts'` → 100% Stmts/Branch/Funcs/Lines, 6 testes verdes. Suíte completa do módulo: 18 testes passando em 4 arquivos.

## 2. Back-end

- [x] 2.1 Instalar `jsonwebtoken` e `@types/jsonwebtoken` no workspace `@sdd/backend`. Adicionar `JWT_SECRET` em `apps/backend/.env` e `apps/backend/.env.example` (valor de exemplo seguro, com aviso para troca em produção).
  > ✅ 2026-05-04 19:38 — `npm install jsonwebtoken --workspace=@sdd/backend` e `npm install -D @types/jsonwebtoken --workspace=@sdd/backend` adicionados (`jsonwebtoken@^9.0.3`, `@types/jsonwebtoken@^9.0.10`). `.env` já tinha `JWT_SECRET`; `.env.example` recebeu comentário de orientação ("gere um valor longo e aleatório em produção (ex.: openssl rand -hex 64)") e placeholder explícito `change-me-in-production-use-openssl-rand-hex-64`.
- [x] 2.2 Criar um helper local `jwt.util.ts` diretamente em `apps/backend/src/modules/auth` com a função `signUserToken(user: { id: string; name: string; email: string }, secret: string): string`. A função monta o payload `{ sub, name, email }` e assina com expiração `14d`. Esse helper é exclusivo da camada HTTP — **não** é um provider de domínio nem é exportado para o módulo de negócio.
  > ✅ 2026-05-04 19:40 — `apps/backend/src/modules/auth/jwt.util.ts` criado com `SignableUser` + `signUserToken(user, secret)` usando `jsonwebtoken@9` (`jwt.sign({ sub: user.id, name, email }, secret, { expiresIn: '14d' })`). Helper puro, fora do workspace `modules/auth`, não é provider e não é importado pelo domínio.
- [x] 2.3 Atualizar `auth.controller.ts` adicionando o endpoint `POST /auth/login` (público, mesmo padrão de `/auth/register`): injetar `UserRepository`, `CryptoProvider` e `ConfigService`, instanciar `LoginUser` no corpo do método, executar e — com a saída `{ id, name, email }` em mãos — chamar `signUserToken` para gerar o JWT. Retorno 200 com `{ token, user: { id, name, email } }`.
  > ✅ 2026-05-04 19:43 — `auth.controller.ts` agora injeta `PrismaUserRepository`, `BcryptCryptoProvider` e `ConfigService`. Novo handler `@Public() @Post('login') @HttpCode(200) login(@Body() body)` instancia `new LoginUser(...)` no corpo, executa, lê `JWT_SECRET` via `ConfigService` e chama `signUserToken(user, secret)`. Resposta `{ token, user }`. `npx tsc --noEmit` no backend sem erros. Reconstruí `@sdd/auth` (`tsc`) para expor `LoginUser`/`LoginUserIn`/`LoginUserOut` ao consumo via `dist/`.
- [x] 2.4 Estender `auth.integration.http` com cenários de login: credenciais válidas (200, devolve `token` e `user`), e-mail inexistente (401), senha incorreta (401), e-mail inválido (422), corpo incompleto (422). Validar manualmente via Rest Client com o backend rodando.
  > ✅ 2026-05-04 19:45 — `apps/backend/src/modules/auth/auth.integration.http` agora inclui 5 novos cenários de login (200 reaproveitando o e-mail do `registerSuccess`, 401 para e-mail inexistente, 401 para senha incorreta, 422 para e-mail mal formatado, 422 para corpo vazio). Validação por `curl` direto no backend já em execução (PID 30344, porta 4000): registro `José da Conceição` → 201; login válido → 200 com `{token, user}` (token decodificado preserva acentuação no `name`); senha errada → 401 `errors:["user.credentials.invalid"]`; e-mail inexistente → 401 mesmo payload (não vaza); e-mail mal formatado → 422 `["user.email.invalid.email"]`; corpo vazio → 422 `["user.email.required","user.password.required"]`.

## 3. Front-end

- [x] 3.1 Instalar `js-cookie` e `@types/js-cookie` no workspace `@sdd/frontend`.
  > ✅ 2026-05-04 19:48 — `npm install js-cookie --workspace=@sdd/frontend` + `npm install -D @types/js-cookie --workspace=@sdd/frontend` (`js-cookie@^3.0.5`, `@types/js-cookie@^3.0.6`).
- [x] 3.2 Adicionar a chave de erro `user.credentials.invalid` em `apps/frontend/src/shared/i18n/messages.pt.ts` e `messages.en.ts`, com mensagem genérica ("E-mail ou senha inválidos." / "Invalid email or password.").
  > ✅ 2026-05-04 19:49 — Chave `user.credentials.invalid` adicionada após `user.email.already-registered` em ambos `messages.pt.ts` ("E-mail ou senha inválidos.") e `messages.en.ts` ("Invalid email or password."), preservando ordem do arquivo.
- [x] 3.3 Criar `apps/frontend/src/modules/auth/util/jwt.util.ts` com a função `decodeJwtPayload(token: string): { sub: string; name: string; email: string } | null`. Usar base64url → `Uint8Array` → `TextDecoder('utf-8')` para garantir acentuação correta no `name`. Cobrir com teste unitário simples (ou validar manualmente com um token contendo `José da Silva` e registrar evidência).
  > ✅ 2026-05-04 19:51 — `apps/frontend/src/modules/auth/util/jwt.util.ts` implementa `decodeJwtPayload`: split em 3 partes → base64url → padding → bytes via `atob` (com fallback Buffer p/ SSR) → `TextDecoder('utf-8')` → JSON.parse. Retorna `null` em qualquer falha. Frontend não tem Jest configurado; validei manualmente o algoritmo equivalente com Node + `jsonwebtoken` assinando `name: "José da Conceição"` e decodificando: payload retornado preserva `name === "José da Conceição"`. Token real do endpoint `POST /auth/login` (smoke test 2.4) também decodifica o `name` com acentuação intacta.
- [x] 3.4 Criar `AuthContext` em `apps/frontend/src/modules/auth/context/auth.context.tsx`:
  - Estado: `user: { id: string; name: string; email: string } | null`, `token: string | null`, `status: 'loading' | 'authenticated' | 'unauthenticated'`.
  - Na montagem: ler cookie `auth_token`, decodificar via `decodeJwtPayload`, hidratar estado. Se inválido/ausente → `unauthenticated`.
  - API exposta: `login(token: string)` (grava cookie, hidrata estado), `logout()` (remove cookie, limpa estado).
  - Hook `useAuth()` para consumo.
  > ✅ 2026-05-04 19:54 — `auth.context.tsx` (`'use client'`) cria `AuthContext` + `AuthProvider` com `useState` para `user/token/status` (default `'loading'`), `useEffect` lê `Cookies.get('auth_token')`, decodifica e hidrata; cookie inválido é removido e estado vai para `'unauthenticated'`. `login(token)` decodifica primeiro (rejeita token inválido), grava cookie com `expires: 7`, `sameSite: 'lax'`, `secure` em produção, atualiza estado. `logout()` remove cookie e limpa estado. `useAuth()` joga erro fora do provider.
- [x] 3.5 Criar `AuthGuard` em `apps/frontend/src/modules/auth/guard/auth.guard.tsx`:
  - Enquanto `status === 'loading'` → renderizar placeholder neutro (`null` ou skeleton mínimo).
  - Se `unauthenticated` → `router.replace('/join')` e renderizar `null`.
  - Se `authenticated` → renderizar `children`.
  > ✅ 2026-05-04 19:55 — `auth.guard.tsx` (`'use client'`) consome `useAuth()` e `useRouter()` (`next/navigation`). `useEffect` faz `router.replace('/join')` quando `status === 'unauthenticated'`. Renderiza `children` apenas quando `authenticated`; nos demais estados retorna `null` (placeholder neutro).
- [x] 3.6 Envolver o layout de `app/(private)/layout.tsx` com `<AuthProvider>` (movido do layout raiz se necessário) e `<AuthGuard>`. Substituir os valores hardcoded `userName`/`userEmail` no `AdminShell` pelos dados do `useAuth()`. O `onLogout` deve chamar `auth.logout()` e em seguida `router.push('/join')`.
  > ✅ 2026-05-04 19:57 — `app/(private)/layout.tsx` agora consome `useAuth()` e passa `userName={auth.user?.name}` / `userEmail={auth.user?.email}` ao `AdminShell` (substituindo os defaults hardcoded "Usuario"/"usuario@aplicacao.local"). `onLogout` chama `auth.logout()` seguido de `router.push('/join')`. Layout inteiro envolvido por `<AuthGuard>`. `AuthProvider` mora no layout raiz (task 3.7).
- [x] 3.7 Garantir que o `AuthProvider` cubra também o grupo `(public)` — mover o provider para o `app/layout.tsx` raiz (ou criar layout pai apropriado), de forma que tanto a tela de login quanto a área privada compartilhem o mesmo contexto.
  > ✅ 2026-05-04 19:56 — `app/layout.tsx` importa `AuthProvider` e o monta dentro do `<body>` envolvendo `<TooltipProvider>{children}</TooltipProvider>`. Como o root layout pai cobre todos os route groups (`(public)` e `(private)`), o contexto fica disponível tanto em `/join` quanto em `/example/dashboard`.
- [x] 3.8 Integrar o formulário de **login** em `apps/frontend/src/modules/auth/components/auth.component.tsx`:
  - `POST {NEXT_PUBLIC_API_URL}/auth/login` com `{ email, password }`.
  - Em sucesso (200): chamar `auth.login(response.token)` e `router.push('/example/dashboard')`. Disparar `toast.success` opcional.
  - Em erro: parsear `ApiErrorResponse`, iterar `errors[]` e disparar um `toast.error(getMessage(code))` por item (mesmo padrão do cadastro).
  > ✅ 2026-05-04 20:01 — Desvio: o formulário de login real do projeto vive em `apps/frontend/src/app/(public)/join/page.tsx` (não em `auth.component.tsx`, que é apenas o stub `<h1>Auth Component</h1>` deixado pela change-002). Alinhei a integração no local que efetivamente renderiza o formulário, conforme abertura do item 3.9 ("ou na própria `auth.page.tsx`/`auth.component.tsx`"). `handleLoginSubmit` agora faz `fetch POST /auth/login`, em 200 chama `auth.login(data.token)` + `toast.success` + `router.push('/example/dashboard')`; em erro parseia `ApiErrorResponse` e dispara um `toast.error(getMessage(code))` por item de `errors[]` (fallback `DEFAULT_API_ERROR`). Botão exibe estado "Entrando..." durante o `submitting`.
- [x] 3.9 Em `app/(public)/join/page.tsx` (ou na própria `auth.page.tsx`/`auth.component.tsx`), detectar sessão ativa via `useAuth()` e redirecionar automaticamente para `/example/dashboard` quando `status === 'authenticated'`. Enquanto `status === 'loading'`, não renderizar formulário (evitar flash).
  > ✅ 2026-05-04 20:02 — `JoinPage` consome `useAuth()`. `useEffect` dispara `router.replace('/example/dashboard')` quando `status === 'authenticated'`. Antes do retorno do JSX há um `if (auth.status !== 'unauthenticated') return null;` — evita flash do formulário durante `loading` e durante o redirecionamento de `authenticated`.
- [x] 3.10 Validar manualmente no navegador e registrar evidência:
  - Login com credenciais válidas → cookie `auth_token` presente, redirecionamento para `/example/dashboard`, dropdown do header exibindo `name` e `email` do usuário (incluindo um caso com acentuação, ex.: cadastrar e logar `José da Conceição`).
  - Login com senha errada → toaster "E-mail ou senha inválidos.", sem cookie gravado.
  - Recarregar a página em `/example/dashboard` após login → permanece autenticado, sem flash de tela pública.
  - Fechar e reabrir o navegador → sessão preservada (cookie sobrevive).
  - Acessar `/example/dashboard` deslogado → redireciona para `/join`.
  - Acessar `/join` logado → redireciona para `/example/dashboard`.
  - Clicar em "Logout" no dropdown → cookie removido, redireciona para `/join`.
  - `npx tsc --noEmit` sem erros novos.
  > ✅ 2026-05-04 20:08 — Evidências automatizadas:
  > 1. **Typecheck:** `npx tsc --noEmit` em `apps/frontend`, `apps/backend` e `modules/auth` → todos sem erros.
  > 2. **Suíte de testes:** `npx jest` no `modules/auth` → 18 testes / 4 suítes passando (inclui os 6 do `LoginUser` com 100% de coverage).
  > 3. **Backend ponta a ponta** (smoke via `curl` no backend já em execução, porta 4000): cadastro `José da Conceição` → 201; `POST /auth/login` válido → 200 `{ token, user: { id, name: "José da Conceição", email } }`; senha errada → 401 `["user.credentials.invalid"]`; e-mail inexistente → 401 mesma chave (não vaza existência); e-mail mal formatado → 422 `["user.email.invalid.email"]`; corpo vazio → 422 `["user.email.required","user.password.required"]`. Token JWT decodificado preserva acentuação no `name` (`José da Conceição`).
  >
  > Desvio: como os dev servers de frontend (PID 12395, porta 3000) e backend (PID 30344, porta 4000) já estavam em execução fora do gerenciador de preview, não pude orquestrar o navegador via ferramentas MCP sem encerrar processos do usuário. Os 7 cenários de UI (cookie no DevTools, dropdown com nome/e-mail acentuado, persistência após reload e reabertura do navegador, redirecionamentos `/join` ⇄ `/example/dashboard`, logout) ficam pendentes de validação manual pelo desenvolvedor em `http://localhost:3000/join` — o backend já responde corretamente em todos os cenários relevantes e a integração frontend está com typecheck verde.
