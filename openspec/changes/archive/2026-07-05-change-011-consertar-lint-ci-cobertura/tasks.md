## Instruções Compartilhadas

Estas instruções valem para qualquer change deste projeto e devem ser respeitadas durante a execução das tasks abaixo:

- [Como executar](../../shared/como-executar.md) — regras de execução e formato de evidência por task.
- [Regras de nomenclatura](../../shared/regras-de-nomenclatura.md) — convenções de nomes de arquivos e diretórios.

## 1. Remover o scaffold morto do `create-turbo`

- [x] 1.1 Confirmar via grep, em todos os `package.json` de `apps/*` e `modules/*`, que nada referencia `@sdd/ui`, `@sdd/eslint-config` ou `@sdd/typescript-config`. Se algo inesperado aparecer, parar e reportar em vez de remover.
  > ⚠️ 2026-07-05 — Grep original cobriu apenas `package.json` (procurando `@sdd/ui`, `@sdd/eslint-config`, `@sdd/typescript-config` como nome de pacote npm) — nada encontrado, mas essa verificação foi **insuficiente**: `packages/typescript-config/base.json` era referenciado via `extends` por **path relativo** (`"../../packages/typescript-config/base.json"`, não pelo nome do pacote) em `packages/shared/tsconfig.json`, `modules/auth/tsconfig.json`, `modules/catalog/tsconfig.json`, `modules/settings/tsconfig.json` e `modules/dashboard/tsconfig.json` — 5 pacotes reais dependiam dele. Descoberto só depois da remoção (task 1.2), quando `check-types` desses 5 pacotes passou a falhar com `TS5083: Cannot read file '.../packages/typescript-config/base.json'`. Corrigido na task 1.2 abaixo. Lição registrada: checagem de "nada usa X" precisa cobrir `extends` de tsconfig, não só `package.json`.
- [x] 1.2 Remover `packages/ui`, `packages/eslint-config` e `packages/typescript-config` por inteiro.
  > ⚠️ 2026-07-05 — `rm -rf packages/ui packages/eslint-config packages/typescript-config` executado. `packages/typescript-config` **era usado de verdade** (ver nota da task 1.1) — como o projeto não tem git, não havia como reverter a remoção; reconstruí `packages/typescript-config/base.json` do zero (conteúdo padrão do template `create-turbo`: `target: es2022`, `module`/`moduleResolution: NodeNext`, `strict`, `esModuleInterop`, `skipLibCheck`, etc.) e validei rodando `tsc --noEmit`, `tsc` (build completo) e a suíte de testes (`npm run test`) dos 5 pacotes afetados (`packages/shared`, `modules/auth`, `modules/catalog`, `modules/settings`, `modules/dashboard`) — todos voltaram a passar exatamente como antes (mesma contagem de testes/suites, cobertura idêntica). `packages/eslint-config` foi confirmado como realmente morto (nenhuma outra referência via grep amplo, incluindo `.mjs`/`.json`/`.ts`, além do próprio `package-lock.json`, que é metadado obsoleto e some no próximo `npm install` limpo). `packages/ui` idem, confirmado morto.
- [x] 1.3 Ajustar `workspaces` do `package.json` raiz e qualquer referência residual em `turbo.json`, se necessário após a remoção.
  > ✅ 2026-07-05 — Nenhum ajuste necessário: `workspaces` raiz usa glob (`["apps/*", "modules/*", "packages/*"]`), continua válido com `packages/shared` sozinho. `turbo.json` não referencia pacotes específicos, só definições de task.
- [x] 1.4 Rodar `npm install` na raiz e confirmar que `npx turbo run lint` e `npx turbo run check-types` não crasham mais (mesmo que ainda só cubram `apps/frontend`/`apps/backend` nesta etapa).
  > ⚠️ 2026-07-05 — `npm install` limpo (node_modules removido e reinstalado do zero) confirmado. `@sdd/backend:lint` passa. `@sdd/frontend:lint` continua falhando, mas por um bug **pré-existente e não relacionado** à remoção dos pacotes desta change: `eslint-config-next` está hoisted na raiz de `node_modules`, enquanto `next` ficou aninhado em `apps/frontend/node_modules` (resquício de quando o `create-next-app` rodou antes do workspace existir) — `package-lock.json` já registrava esse layout antes desta change, e um reinstall completo reproduz o mesmo resultado (não é algo que a remoção dos pacotes órfãos causou). Duas tentativas de correção foram propostas ao usuário (reinstall completo — feito, não resolveu; adicionar `next` também na raiz do `package.json` — ação recusada pelo usuário). **Decisão**: registrar como limitação conhecida e seguir com o restante da change; o lint do `apps/frontend` fica de fora do escopo resolvido aqui, sem bloquear as demais tasks. `npx turbo run check-types` não crasha mais, mas achado à parte: nenhum workspace real (`apps/frontend`, `apps/backend`, `packages/shared`, `modules/*`) tem o script `check-types` de fato — só o extinto `@sdd/ui` tinha. Endereçado na task 1.5 nova, abaixo.
- [x] 1.5 (Nova, adicionada durante a implementação) Adicionar o script `"check-types": "tsc --noEmit"` a `apps/frontend`, `apps/backend`, `packages/shared` e cada `modules/*`, já que nenhum deles o tinha antes desta change — sem isso, `turbo run check-types` não executa nada de verdade em nenhum workspace real.
  > ✅ 2026-07-05 — Script adicionado nos 7 `package.json` reais. `npx turbo run check-types` agora executa de fato em todos eles.

## 2. Lint na camada de domínio (`packages/shared` + `modules/*`)

- [x] 2.1 Criar `packages/shared/eslint.config.mjs` (flat-config ESLint 9: `eslint.configs.recommended` + `tseslint.configs.recommendedTypeChecked` + `eslint-plugin-prettier/recommended`, `globals.node`/`globals.jest`), instalar as `devDependencies` de lint necessárias (`eslint`, `@eslint/js`, `typescript-eslint`, `globals`, `eslint-plugin-prettier`, `eslint-config-prettier`) e adicionar o script `"lint": "eslint ."`.
  > ✅ 2026-07-05 — Config criado espelhando `apps/backend/eslint.config.mjs`. Precisou também de `tsconfig.eslint.json` por pacote (`extends: ./tsconfig.json`, `include` ampliado para `src`, `test`, `jest.config.ts`) porque o `tsconfig.json` de build restringe `include: ["src"]` só para o `tsc` de produção — sem isso, `parserOptions.project` do ESLint não enxergava os arquivos de teste (erro "not found by the project service"). `eslint.config.mjs` referencia `project: './tsconfig.eslint.json'` em vez de `projectService: true`. Versões de dependência espelhadas exatamente das já usadas por `apps/backend` (evita duas versões resolvidas de eslint/typescript-eslint na árvore).
- [x] 2.2 Repetir a task 2.1 para `modules/auth`, `modules/catalog`, `modules/settings` e `modules/dashboard`.
  > ✅ 2026-07-05 — Mesmo `eslint.config.mjs`/`tsconfig.eslint.json` copiados para os 4 módulos. `modules/dashboard` não tem `test/`/`jest.config.ts` (módulo somente-leitura sem caso de uso, decisão da change-010) — o `include` amplo não encontra nada para esses paths, sem erro.
- [x] 2.3 Rodar o lint de cada pacote (`packages/shared`, `modules/auth`, `modules/catalog`, `modules/settings`, `modules/dashboard`) e corrigir toda violação real encontrada — sem desabilitar regra em massa para o comando passar.
  > ✅ 2026-07-05 — `eslint --fix` resolveu toda formatação (`prettier/prettier`). Violações reais corrigidas uma a uma: (1) `require-await` em ~30 métodos de fakes/mocks (`create`/`update`/`delete`/`findById`/`findPage`/etc. em `test/mock/fake-*.ts` de `auth`, `catalog`, `settings`, e um `mockImplementation` em `create-order.usecase.test.ts`) — removido `async` desnecessário, retorno explícito via `Promise.resolve(...)`, mesmo shape observável; (2) `no-empty-object-type` em 4 interfaces de repositório (`BannerRepository`, `CategoryRepository`, `ProductRepository`, `StoreSettingsRepository`, todas `interface X extends CrudRepository<...> {}`) — em vez de disable por arquivo, ajustada a regra no `eslint.config.mjs` compartilhado para `allowInterfaces: 'with-single-extends'` (permite esse padrão documentado na skill `module-repository`, continua pegando interface vazia sem nenhum extends); (3) `no-base-to-string` em `rule.utils.ts` (fallback deliberado de stringificação) e `no-unsafe-call`/`no-unsafe-assignment` em `age.rule.test.ts` (acesso a método privado via cast, num teste de borda) — ambos com `eslint-disable-next-line` pontual e comentário explicando o motivo.
- [x] 2.4 Depois de qualquer correção de código motivada pelo lint, rodar a suíte de testes do pacote afetado e confirmar que nada quebrou.
  > ✅ 2026-07-05 — `npm run test --workspace @sdd/shared/@sdd/auth/@sdd/catalog/@sdd/settings`: mesma contagem de testes/suites de antes das correções (shared 96/96, auth 29/29, catalog 63/63, settings 10/10). Nenhuma regressão.
- [x] 2.5 Rodar `npx turbo run lint` e `npx turbo run check-types` na raiz e confirmar que agora cobrem `apps/frontend`, `apps/backend`, `packages/shared` e todos os `modules/*`, sem erro.
  > ⚠️ 2026-07-05 — Achado extra: `check-types` de `apps/backend` falhou com `Property 'storeSettings' does not exist on type 'PrismaService'` (5 erros) — o Prisma Client gerado (`node_modules/@prisma/client`) ficou desatualizado depois do `rm -rf node_modules` + reinstall da task 1.4 (o projeto não tem `postinstall: prisma generate`, então o client não se regenera sozinho após reinstalar). Corrigido com `npx prisma generate` em `apps/backend`. **Implicação para a task 4 (CI)**: o workflow precisa rodar `npx prisma generate` logo após `npm ci` e antes de `check-types`/`build`/`test`, senão qualquer checkout novo (CI, ou um dev clonando o repo do zero) reproduz o mesmo erro. Depois desse fix, `check-types` passa em todos os 7 workspaces. `lint` passa em `apps/backend`, `packages/shared` e todos os `modules/*` — `apps/frontend` continua bloqueado pelo bug de hoisting `next`/`eslint-config-next` documentado na task 1.4 (pré-existente, fora do escopo resolvido nesta change).

## 3. Cobertura real antes de travar o threshold

- [x] 3.1 Rodar `npm run test --workspace @sdd/shared -- --coverage` e ler o relatório real de cobertura.
  > ✅ 2026-07-05 — 100% em todo `src/**` (96/96 testes). Único ponto abaixo de 100% era `test/mock` (fakes de teste), não código de domínio.
- [x] 3.2 Repetir a task 3.1 para `@sdd/auth`, `@sdd/catalog` e `@sdd/settings`. (`@sdd/dashboard` não tem `test`/jest configurado — módulo somente-leitura sem entidade nem caso de uso, conforme decisão já registrada na change-010; não criar suíte de teste artificial só para esta change.)
  > ✅ 2026-07-05 — `@sdd/auth`: 100% em `src/**` (29/29 testes), gap só em `test/mock/fake-user.repository.ts`. `@sdd/settings`: 100% em `src/**` (10/10 testes), gap só em `test/mock/fake-store-settings.repository.ts`. `@sdd/catalog`: 3 gaps REAIS em `src/**` encontrados (não em mocks) — ver task 3.3.
- [x] 3.3 Para cada pacote com lacuna real de cobertura, fechar a lacuna com teste de verdade quando for razoável nesta entrega. Se algum gap for grande demais para fechar agora, documentar aqui o pacote, o arquivo e o motivo específico do relaxamento pontual do threshold.
  > ✅ 2026-07-05 — **Decisão adicional durante a implementação**: todos os gaps restantes em todos os 4 pacotes eram em `test/mock/fake-*.ts` (repositórios fake usados só para apoiar os testes de caso de uso, não código de domínio). Em vez de escrever testes artificiais para cobrir ramos irrelevantes de dublês de teste, `collectCoverageFrom` foi restrito a `src/**/*.ts` em cada `jest.config.ts` (task 3.4) — o padrão de 100% das skills (`module-entity`, `module-use-case`, `shared-validation-rule`) é sobre o código de domínio criado, não sobre a infraestrutura de teste que o apoia. Os 3 gaps reais em `src/**` do `@sdd/catalog` foram fechados: (1) `order.entity.ts` — o guard `if (!Array.isArray(this.items)) return;` em `validateItems()` era código morto inalcançável (o `Validator.validate()` anterior já rejeita `items` não-array via `RequiredRule`/`MinItemsRule` antes de chegar lá, para qualquer valor possível — confirmado lendo `isEmptyValue`/`MinItemsRule`) — removido em vez de testado; (2) `create-order.usecase.ts` — faltava teste para o caminho de exaustão de tentativas (`MAX_CODE_ATTEMPTS` colisões seguidas → `DomainError`), teste real adicionado; (3) `save-product.usecase.ts` — faltava teste para atualização informando um novo `categoryId` explicitamente (só havia teste do caminho "omitido, preserva o existente"), teste real adicionado. Nenhum threshold precisou ser relaxado — 100% real em `src/**` nos 4 pacotes.
- [x] 3.4 Adicionar `coverageThreshold: { global: { statements: 100, branches: 100, functions: 100, lines: 100 } }` (ou o valor real justificado pela task 3.3) ao `jest.config.ts` de `packages/shared`, `modules/auth`, `modules/catalog` e `modules/settings`.
  > ✅ 2026-07-05 — Adicionado junto com `collectCoverageFrom: ["src/**/*.ts"]` (ver task 3.3) nos 4 `jest.config.ts`.
- [x] 3.5 Rodar `npm run test --workspace <cada um dos 4 pacotes>` novamente e confirmar que o comando falha se a cobertura cair abaixo do threshold configurado (ex.: comentar temporariamente uma linha de teste e confirmar a falha, depois reverter) e passa no estado atual do código.
  > ✅ 2026-07-05 — Os 4 pacotes passam limpos no estado atual. Validação do enforcement: pulei temporariamente (`test.skip`) o teste novo de exaustão de tentativas em `create-order.usecase.test.ts` — o comando falhou corretamente (`npm error code 1`, "coverage threshold for statements (100%) not met: 99.64%"); revertido e confirmado passando de novo (65/65).

## 4. Pipeline de CI

- [x] 4.1 Criar `.github/workflows/ci.yml`: gatilhos `push` e `pull_request`, job único com checkout, setup Node 20 (LTS), `npm ci`, depois `npx turbo run build check-types lint test`.
  > ✅ 2026-07-05 — Job único: checkout, setup Node 20 com cache npm, `npm ci`, `npx prisma generate` (passo adicional descoberto na task 2.5 — sem ele, um checkout novo reproduziria o erro de `PrismaService` sem `storeSettings`), depois build/check-types/lint/test como steps separados (mais legível no log do GitHub que um comando combinado). Adicionado também `"test"` como task em `turbo.json` (não existia — `turbo run test` falhava com "Could not find task `test` in project" até essa adição) e `"dependsOn": ["^build"]` nela, já que os pacotes de domínio consomem `@sdd/shared` pelo `dist/` compilado (via `package.json` `main`), não pelo `src/` diretamente.
  > ⚠️ O step de lint documentado com comentário no próprio YAML: `@sdd/frontend` pode falhar por causa do bug de hoisting `next`/`eslint-config-next` (task 1.4/2.5) — decisão consciente de deixar o CI refletir o estado real do projeto em vez de mascarar o problema com `continue-on-error`.
- [x] 4.2 Revisar o YAML gerado (sintaxe, indentação, nomes de step) — não há como executar o workflow de fato nesta change (sem git/remote), então a revisão é manual/visual.
  > ✅ 2026-07-05 — Sintaxe validada com `js-yaml` (`yaml.load` sem erro). Cada comando referenciado no YAML (`npm ci`, `npm run prisma:generate --workspace @sdd/backend`, `npx turbo run build/check-types/lint/test`) foi executado localmente nesta sessão, replicando exatamente os steps do workflow, confirmando que funcionam (exceto a exceção conhecida do lint do frontend).

## 5. Verificação final

- [x] 5.1 Rodar `npx turbo run build check-types lint test` na raiz do monorepo uma última vez e confirmar sucesso total (todos os workspaces reais, sem os pacotes removidos na task 1.2).
  > ✅ 2026-07-05 — `build`: 7/7. `check-types`: 7/7. `test`: 9/9. `lint`: 5/7 (exceção conhecida e documentada de `@sdd/frontend`, ver task 1.4/2.5/4.1). Dev servers do usuário (portas 3000 e 4000) confirmados saudáveis ao final (200 em ambos).
- [x] 5.2 Relatar ao usuário: pacotes removidos, pacotes com lint/coverage novos, qualquer threshold relaxado pontualmente (task 3.3) e o estado final do CI (criado, mas inerte até haver git + remote).
  > ✅ 2026-07-05 — Ver resumo entregue ao usuário na conversa.
